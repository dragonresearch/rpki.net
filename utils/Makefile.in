# $Id$

SUBDIRS = uri print_rpki_manifest print_roa hashdir find_roa

all clean test distclean install deinstall uninstall::
	@for i in ${SUBDIRS}; do echo "Making $@ in $$i"; (cd $$i && ${MAKE} $@); done

distclean::
	rm -f Makefile
