# $Id$

CFLAGS	= @CFLAGS@
LDFLAGS = @LDFLAGS@
LIBS	= @LIBS@ -lssl -lcrypto

all: pywrap

pywrap: pywrap.c
	AC_CFLAGS='${CFLAGS}' AC_LDFLAGS='${LDFLAGS}' AC_LIBS='${LIBS}' @PYTHON@ build.py

clean:
	rm -f *.o pywrap

test:
	@true
